spring:
  application:
    name: ClienteAPI
  datasource:
    url: jdbc:postgresql://localhost:5432/sistema_clientes
    username: miguel
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true